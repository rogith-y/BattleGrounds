<html>
	<head>
			
      <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
			<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.0/jquery.min.js"></script>
			<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
			<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
			<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
 <link href="https://fonts.googleapis.com/css?family=Teko" rel="stylesheet">
	</head>

<body>
<style>
  @-webkit-keyframes blink {
  50% {
    opacity: 1;
  }
  100% {
    opacity: 0;
  }
}
@keyframes blink {
  50% {
    opacity: 1;
  }
  100% {
    opacity: 0;
  }
}
body {
  background-image: url("back.png");
  color: #eee;
  font-family: 'Share Tech Mono', monospace;
  -webkit-user-select: none;
     -moz-user-select: none;
      -ms-user-select: none;
          user-select: none;
}

.hidden {
  display: none;
}

.password {
  position: absolute;
  top: 50%;
  left: 40%;
  -webkit-transform: translate(-50%, -50%);
          transform: translate(-50%, -50%);
  font-size: 60px;
  letter-spacing: 5px;
  text-transform: uppercase;
}

.granted {
  position: absolute;
  top: 75%;
  left: 40%;
  -webkit-transform: translate(-50%, -50%);
          transform: translate(-50%, -50%);
  font-size: 30px;
}

.info {
  position: absolute;
  top: 0;
  left: 0;
  font-size: 15px;
}
.info p {
  margin: 10px;
}

.button {
  background-color: #111;
  border: solid 1px #888;
  padding: 8px 25px;
  font-size: 26px;
  letter-spacing: 2px;
  cursor: pointer;
}

.rerun {
  position: absolute;
  bottom: 15px;
  right: 15px;
}

.logout {
  position: absolute;
  bottom: 15px;
  left: 15px;
}

.start {
  position: absolute;
  top: 50%;
  left: 47%;
  -webkit-transform: translate(-50%, -50%);
          transform: translate(-50%, -50%);
}

.blink {
  -webkit-animation: blink 0.8s steps(1, start) infinite alternate;
          animation: blink 0.8s steps(1, start) infinite alternate;
}
 @font-face {
  font-family: 'MyWebFont';
  src:url('font2.otf') format('opentype'); /* Chrome 4+, Firefox 3.5, Opera 10+, Safari 3—5 */
} 

</style>
<script>
  $(document).ready(function(){
    $(".modal").hide();
    $(".logpop").hide();
  })
</script>
<div class="logpop">
  <style>
.modal {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  width: 400px;
  height: 100px;
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  padding: 1rem 2rem;
  border: 3px solid black;
  border-radius: 15px;
  background: rgba(100, 99, 96, 0.808);
  box-shadow: 8px 8px 0 rgba(0, 0, 0, 0.2);
}
.message {
  font-size:1.1rem;
  margin-bottom: 1.6rem;
  margin-top: 0;
}
.btn {
    font-family:inherit;
  font-size: inherit;
  background: white;
  padding: 0.3rem 3.4rem;
  border: 3px solid black;
  margin-right: 2.6rem;
  box-shadow: 0 0 0 black;
  transition: all 0.2s;
}
.yes,.no{
  color:rgb(0, 0, 0);
}
.btn:last-child {
  margin: 0;
}
.btn:hover {
  box-shadow: 0.4rem 0.4rem 0 black;
  transform: translate(-0.4rem, -0.4rem);
}
.btn:active {
  box-shadow: 0 0 0 black;
  transform: translate(0, 0);
}
.options {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
}
  </style>
</div>

<!-- ######  -->
<div class="modal">
  <p class="message">Do you want to Quit?</p>
  <div class="options">
    <button class="btn yes">Yes</button>
    <button class="btn no">No</button>
  </div>
</div>

<!-- ######  -->

<div class="info">
	<p>----WARNING!----<br>You're about to enter a BattleGround,<br>Good Luck for a ChickenDinner!...<br>You can wait or press(hold) any key to continue...
</div>
<center><div style="font-family:'MyWebFont';margin-left:30px;font-size:50px; "class="password hidden"></div></center>
<div class="granted hidden" style="font-family: 'Teko', sans-serif;"><img src="giphy.gif" style="border-radius: 50%;width:150px;height:auto;"></div>
<div class="button rerun hidden" style="font-family: 'Teko', sans-serif;">Start Game</div>
<div class="button logout hidden" style="font-family: 'Teko', sans-serif;">Logout</div>
<script>
  $(".logout").click(function(){
    $(".modal").show();
    $(".logpop").show();
  })
</script>

<!-- ######  -->
<script>
  var passwords = ['ITRIX 19', 'CHICKEN DINNER', 'ITRIX 19','BATTLEGROUNDS', 'BATTLEGROUNDS', 'TAKING OFF SOON','TESTING THE LIMITS'];
var indexOld;
var index = Math.floor((Math.random() * passwords.length));
var password = passwords[index];
var characters = [];
var counter = 0;
	
var interval = setInterval(function(){
		for(i = 0; i < counter; i++) {
			characters[i] = password.charAt(i);
		}
		for(i = counter; i < password.length; i++) {
			characters[i] = Math.random().toString(36).charAt(2);
		}
		$('.password').text(characters.join(''));
	}, 25);
	
hack();
function hack() {
  setInterval(function(){
    counter++;
  },300); 
}
function hack1() {
	counter++;
	if(counter == password.length){
		$('.granted, .rerun,.logout').removeClass('hidden');
	}
}
$(window).on('keydown', hack1);
$('.password').on('click', hack1);
$('.password').on('click', hack1);
setInterval(function(){
  $('.granted, .rerun,.logout').removeClass('hidden');
},2800);

$('.rerun').on('click', function() {
	//prevent from displaying the same password two times in a row
	indexOld = index; 
	do {
		index = Math.floor((Math.random() * passwords.length));
	} while(index == indexOld);
	
location.href="/home";
});
$('.no').click(function(){
  $('.modal').hide();
})
$('.yes').on('click', function() {
	//prevent from displaying the same password two times in a row
	indexOld = index; 
	do {
		index = Math.floor((Math.random() * passwords.length));
	} while(index == indexOld);
	
location.href="/logout";
});

//keyboard events won't fire if the iframe isn't selected first in Full Page view
$(document).ready(function(){
$(this).addClass('hidden');
	$('.info p:last-child, .password').removeClass('hidden');
});
</script>

      
	  </body>
	  </html>